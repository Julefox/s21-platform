// ============================================================================
//  SHARED autoexec (client + dedi). CHostState::LoadConfig always execs this
//  first (unless -launcher >= 1 or -devsdk). Role-specific files follow:
//    dedi:   system/autoexec_server.cfg + tools/rcon_server.cfg
//    client: system/autoexec_client.cfg + tools/rcon_client.cfg
// ============================================================================

mp_allowed                   "1"
