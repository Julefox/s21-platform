// ============================================================================
//  CLIENT autoexec (r5apex + client.dll). Exec'd after system/autoexec.cfg
//  by CHostState::LoadConfig when !DEDICATED.
// ============================================================================

cl_noTimeoutLocalHost        "0"
origin_disconnectWhenOffline "0"
